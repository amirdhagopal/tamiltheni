// Basic sanity check to ensure BAT folder is active
import { describe, it, expect } from 'vitest';

describe('BAT Setup', () => {
    it('should be discovered by vitest', () => {
        expect(true).toBe(true);
    });
});
